# Byte-compiled
__pycache__/
*.py[cod]
*.pyd

# Local dependency caches / scratch (never commit)
.deps/
.pylibs_new/
.pylibs/
.cache/
.tmp/
.codex-review/

# Environment
.env
.venv
env/
venv/

# Streamlit secrets (API keys — NEVER commit)
.streamlit/secrets.toml

# Google service account keys — NEVER commit
*.json
!exports/warning_dashboard_summary.json
!market_stage_model/top100_scan_snapshot.json
!data/Strategy_Comparisons/*.csv

# Runtime data files (auto-generated, not source code)
data/daily_run_log.json
data/sentiment_cache.json
data/system_e_signal_cache.json
exports/artifact_snapshot.json

# market_data_master.parquet IS committed (3.8 MB): Streamlit Cloud boots with it
# and only fetches the incremental delta instead of re-downloading 22 years.

# R3 signal cache IS needed by the deployed dashboard (small, refreshed Thu)
!data/r3_signal_cache.json

# Live trading records (E80/R3 paper ledger since 2026-07-01) — keep all
!data/live_trading/r3_signal_history.json

# Trap regime backtest: ignore bulky raw data, but KEEP dashboard inputs
exports/trap_regime_backtest/*
!exports/trap_regime_backtest/risk_allocation/
exports/trap_regime_backtest/risk_allocation/*
!exports/trap_regime_backtest/risk_allocation/*.csv
!exports/trap_regime_backtest/system_e_weekly_signals.csv
!exports/trap_regime_backtest/system_e_equity_curves.csv
!exports/trap_regime_backtest/system_e_performance_summary.csv

# Logs
*.log

# IDE
.vscode/
